@model IEnumerable<VeterinaryClinic.Entities.Entities.DocumentType>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.NameDocumentType)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.NameDocumentType)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.IdDocumentType">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.IdDocumentType">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.IdDocumentType">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
